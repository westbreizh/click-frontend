// Création d'un objet de configuration
const config = {
  backendURL: 'https://click-backend.herokuapp.com/', // Propriété "backendURL" contenant l'URL de votre backend
};

// Exportation de l'objet de configuration pour le rendre disponible ailleurs dans le code
module.exports = config;
